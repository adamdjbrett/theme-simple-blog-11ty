<ul class="postList">
  {{ for post of postslist }}
  <li class="post">
    <h2 class="post-title">
      <a href="{{ post.url }}" {{ if post.url == page.url }} aria-current="page"{{ /if }}>
        {{ post.data.title || post.url }}
      </a>
    </h2>

    {{ include "templates/post-details.vto" {
      date: post.date,
      tags: post.data.tags,
      author: post.data.author,
      readingInfo: post.data.readingInfo
    } }}
  </li>
  {{ /for }}
</ul>
